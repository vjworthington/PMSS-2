<?xml version="1.0" encoding="UTF-8"?>

<configuration>

    <!-- Normal application logging to the console -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">

        <encoder>
            <pattern>
                %d{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n
            </pattern>
        </encoder>

    </appender>

    <!-- Dedicated PMSS security log -->
    <appender name="SECURITY_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">

        <file>logs/pmss-security.log</file>

        <encoder>
            <pattern>
                %d{yyyy-MM-dd HH:mm:ss} %-5level %msg%n
            </pattern>
        </encoder>

        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">

            <fileNamePattern>
                logs/pmss-security.%d{yyyy-MM-dd}.%i.log
            </fileNamePattern>

            <maxFileSize>10MB</maxFileSize>
            <maxHistory>30</maxHistory>

        </rollingPolicy>

    </appender>


    <!-- Only PMSS_SECURITY goes into pmss-security.log -->
    <logger name="PMSS_SECURITY" level="INFO" additivity="false">

        <appender-ref ref="SECURITY_FILE"/>

    </logger>

    <!-- Everything else goes to the console -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>

</configuration>